export interface Breadcrumb {
    title: string;
    route: string;
}

export interface ICustomerDetails {
    route: string,
    isDraft?: string,
    isEnable: boolean
  }

export interface ICustomerData {
    name: string,
    id: string
  }